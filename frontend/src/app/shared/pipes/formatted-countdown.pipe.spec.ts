import { FormattedCountdownPipe } from './formatted-countdown.pipe';

describe('FormattedCountdownPipe', () => {
  it('create an instance', () => {
    const pipe = new FormattedCountdownPipe();
    expect(pipe).toBeTruthy();
  });
});
