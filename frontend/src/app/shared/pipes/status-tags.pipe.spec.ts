import { StatusTagsPipe } from './status-tags.pipe';

describe('StatusTagsPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusTagsPipe();
    expect(pipe).toBeTruthy();
  });
});
